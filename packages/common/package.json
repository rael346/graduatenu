{
  "name": "@graduate/common",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "yarn build:dev -w",
    "build": "yarn build:dev && yarn build:browser && yarn build:module",
    "build:dev": "yarn g:babel --root-mode upward -d dist -x '.ts' *.ts",
    "build:browser": "yarn g:cross-env BABEL_ENV=browser yarn g:babel --root-mode upward -d lib -x '.ts' *.ts",
    "build:module": "yarn g:cross-env BABEL_ENV=module yarn g:babel --root-mode upward -d module -x '.ts' *.ts"
  },
  "devDependencies": {
    "axios": "^0.26.1",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "@babel/plugin-proposal-class-properties": "^7.16.7",
    "@babel/plugin-proposal-decorators": "^7.17.8",
    "@nestjs/mapped-types": "^1.0.1",
    "class-validator": "^0.13.2",
    "dataloader": "^1.4.0"
  }
}
